[pytest]
testpaths = tests
# El plugin global pytest-vcr instalado en esta máquina revienta al importarse
# (incompatibilidad con la urllib3 del entorno) y no lo usa nada de este proyecto.
addopts = -p no:vcr -q
